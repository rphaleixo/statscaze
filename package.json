{
  "name": "statscaze",
  "version": "1.0.0",
  "private": true,
  "description": "Coleta e análise de performance de vídeos do YouTube (Cazé TV), rodando como Cloudflare Worker + D1.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply statscaze --local",
    "db:migrate:remote": "wrangler d1 migrations apply statscaze --remote"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
